# MIKU v0.1 — Core Brain dependencies
# STATUS: PARTIAL — install before running any script
#
# Install with:
#   pip install -r requirements.txt
# Or with uv:
#   uv pip install -r requirements.txt

# Core ML
torch>=2.0.0               # transformer, autograd, SDPA (flash-attn on 2.x)
numpy>=1.24.0              # array ops, mmap binary storage

# Tokenizer (from-scratch BPE trainer — NOT a pretrained vocabulary)
sentencepiece>=0.1.99

# Config + utilities
pyyaml>=6.0                # YAML config loading
tqdm>=4.65.0               # progress bars
matplotlib>=3.7.0          # loss curve plots (saved to disk, not shown interactively)
setuptools>=83.0.0         # packaging and build utilities
